class Meetup
  def initialize(month, year)
    @month = month
    @year = year
  end

  def day(weekday, schedule)
    case schedule

  end
end

# day(weekday, schedule)
# based on the weekday and schedule, need to find
# a date.

# We know the month and the year, so we can determine the
# number of days in a given month and on what day they land...
# but the question is, how do we do THAT?
